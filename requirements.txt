# Native desktop GUI framework (LGPL, packages cleanly with PyInstaller)
PySide6==6.7.2

# Local config / secrets loading
python-dotenv==1.0.1

# Remote (Oracle Cloud) control over SSH — used when PTA_MODE=remote
paramiko==3.4.0

# Nothing else is required — SQLite access uses Python's built-in sqlite3
